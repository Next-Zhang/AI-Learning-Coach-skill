# AI-Native Learning Tool

一个以 AI 为核心的学习工具，围绕「计划 → 执行 → 反馈 → 验收 → 总结」的闭环运转，让 AI 充当学习教练：生成学习计划、监督执行、辅助实操、持久化进度。

## Language

**学习计划 (Learning Plan)**:
AI 为学习者生成的、按天划分的学习安排。
_Avoid_: 课程表, syllabus

**每日任务 (Daily Task)**:
学习计划中一天的执行单元，是计划与执行之间的最小粒度。
_Avoid_: 课时, lesson

**进度反馈 (Progress Feedback)**:
学习者完成每日任务后向 AI 提供的进展信息，是 AI 监督与辅助的依据。
_Avoid_: 打卡, check-in

**学习进度 (Learning Progress)**:
跨日累积的持久化学习记录，由每日总结追加而成，是推进学习计划的依据。
_Avoid_: 历史记录, chat history

**用户画像 (User Profile)**:
对学习者的测评与观察记录：一次性 onboarding 问卷打底（目标、水平自评、时间预算、风格偏好、压力自评），每轮验收的难度反馈增量修正。用于计划适配性输入。
_Avoid_: 心理测评档案

**计划验证 (Plan Verification)**:
对学习计划的检验，三个维度：真实性（计划内容有可靠出处，靠 RAG 检索核查）、合理性（主题顺序与每日份量教学上成立，靠独立评审 agent）、适配性（符合该学习者的目标、水平与时间预算，靠用户画像输入）。

**学习目标 (Learning Goal)**:
学习者用一句话说清的"想用 Python 做什么"的具体目标；计划生成的第一步，驱动子领域圈定与范围声明。

**范围声明 (Scope Declaration)**:
学习计划显式声明的边界——本计划覆盖哪些子领域、不涉及什么（如"覆盖数据分析方向，不涉及 Web 框架"）。用于检索源选择、验证评审对象、失控防护锚点。

**混合式验收 (Hybrid Acceptance)**:
每日任务的完成判定流程：学习者提交当日证据 → AI 对照当日目标核查并给结论（完成/部分完成/未完成）→ 学习者确认 → 才写当日总结。纯自报变打卡机，纯 AI 判定易打击信心。

**失控防护 (Runaway Prevention)**:
贯穿设计的硬约束，防四种失控：范围漂移（agent 偏离任务边界加戏）、内容幻觉（无依据编造）、破坏性操作（误删误改持久层文件）、空转循环（卡住反复消耗资源）。落地机制三层面：rules（护栏规则）、permission（DSH 文件沙箱可禁的操作）、approval（破坏性操作必须用户批准）。

**形态演进 (Form Evolution)**:
本工具的载体路线：v0 技能集（在 DeepSeek Harness 内跑通闭环）→ 包装为 plugin → 逐步升级为独立应用。设计上始终保留"闭环逻辑与载体解耦"的余地。

**能力矩阵 (Skill Matrix)**:
摸底测试产出的画像形态——按知识点维度的水平评分（如 pandas: 2/5，语法: 3/5），而非单一总分。是计划适配性输入的核心数据。

**完成度评分 (Completion Score)**:
每日验收时对任务完成情况的量化评分，1–5 分制，按「agent 评分 70% + 学习者自评 30%」合成；难度反馈（太难/刚好/太简单）独立记录，不混入完成度分，只影响后续计划难度档位。

**检索层 (Retrieval Layer)**:
技能内定义的统一检索接口——输入查询，输出资料列表。v0 内部用关键词检索（glob/grep/web_search），升级时替换为向量检索，流程代码不变。数据格式先行、引擎后置。

**资料库 (Sources)**:
带 frontmatter 元数据（标题/来源 URL/主题标签/日期/摘要）的 Markdown 资料集合，存放于 `sources/`。是检索层的 v0 数据源，也是未来向量库的切块数据，升级零迁移。

**上下文管理策略 (Context Strategy)**:
每个学习日新开一个会话，当日结束后旧会话归档不再加载；持久信息通过每日总结写入学习进度。以"不延续"实现上下文管理，而非物理删除历史。

**复习快查文档 (Review Sheet)**:
每节课结束后同步输出的浓缩知识卡，按课程一份存放于 `review/`（如 `review/01-pandas-groupby.md`）。每个知识点一行：概念一句话 + 关键代码/示例 + 常见坑 + 来源引用。用途：用户快速回忆历史知识；agent 依据它出题考察。必须保持用户可读，支持按知识点、按日期直接查阅。

**间隔重复 (Spaced Repetition)**:
复习调度策略，参考艾宾浩斯遗忘曲线：每个知识点按 1天 → 3天 → 7天 → 15天 → 30天 的间隔推进，考察通过则推迟下一轮，未通过则重置回 1 天。由 `review/schedule.md` 调度表驱动，agent 在会话开头检查到期知识点。

**掌握度 (Mastery)**:
按知识点记录的掌握水平，与能力矩阵共用同一套数据结构（知识点 × 评分）。学习验收与复习考察的得分都写回掌握度，是适配性与复习调度的共同输入。

**调度表 (Review Schedule)**:
`review/schedule.md`——知识点 → 掌握度 → 下次复习日的调度记录，与快查文档分离。agent 只查调度表决定复习什么，不扫描全部文档；文档累积不影响性能。
